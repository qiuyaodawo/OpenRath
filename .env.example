# Copy to .env and fill in values. Never commit .env (see .gitignore).

# OpenAI API key (or compatible provider key)
OPENAI_API_KEY=

# API base URL (OpenAI default, or your gateway / proxy)
OPENAI_BASE_URL=https://api.openai.com/v1

# Default chat model when callers omit `model` (provider-specific id)
OPENAI_DEFAULT_MODEL=gpt-5.5

# --- OpenSandbox (local Docker runtime via `opensandbox-server`) ---
# Python SDK env names (defaults to localhost:8080 if OPEN_SANDBOX_DOMAIN unset).
# If you enable `api_key` on the server, set the same secret here and in
# OPENSANDBOX_SERVER_API_KEY (server reads this env and overrides TOML).
OPEN_SANDBOX_DOMAIN=127.0.0.1:8080
OPEN_SANDBOX_API_KEY=
OPENSANDBOX_SERVER_API_KEY=
