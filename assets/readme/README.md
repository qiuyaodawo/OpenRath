# README assets

Images referenced by the root `README.md`, `README_zh.md`, and selected examples.
These files live in the OpenRath repository so README rendering does not depend on the
`docs/` submodule (OpenRath-Docs).

When updating branding or architecture overview images shown on GitHub, edit files here.
Sphinx-specific diagrams remain under `docs/source/_static/` in the docs submodule.
