[pytest]
minversion = 8.0
testpaths = tests
addopts =
    --strict-config
    --strict-markers
    -ra
filterwarnings =
    # Benign: anyio.Event finalizers on Windows occasionally raise
    # ``RuntimeError: Event loop is closed`` during interpreter shutdown.
    ignore::pytest.PytestUnraisableExceptionWarning
